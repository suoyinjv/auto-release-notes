{
  "name": "auto-release-notes",
  "version": "1.0.0",
  "description": "Auto-generate release notes from conventional commits",
  "main": "lib/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist",
    "test": "node test.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
